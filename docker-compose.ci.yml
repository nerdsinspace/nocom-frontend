version: '3.3'
services:
  nocom-frontend:
    image: node:12
    network_mode: host
    environment:
      - NPM_CONFIG_CACHE=~/.npm
    volumes:
      - ./:/opt/nocom-frontend/:Z
    working_dir: /opt/nocom-frontend
    entrypoint: npm
